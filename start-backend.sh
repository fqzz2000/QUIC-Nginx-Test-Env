docker run -d --rm --name nginx-backend  -v $(pwd)/data:/usr/share/nginx/html/files -v $(pwd)/certs:/etc/nginx/certs -v $(pwd)/backend/nginx.conf:/etc/nginx/nginx.conf   --network host   nginx-backend
